/**
 * Build-time constants injected by Vite `define` (see vite.config.ts).
 * In dev/tests they are undefined; code must guard with `typeof` and fall back
 * to the full-game values.
 */
declare const __DEMO__: boolean
declare const __DEMO_MAX_FLOOR__: number
